{
    "$id": "https://raw.githubusercontent.com/magnolia-community/definition-schemas/UI6/dialog.schema.json",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "UI6 Dialog definition",
    "description": "Dialogs for Magnolia CMS https://docs.magnolia-cms.com/product-docs/Templating/Dialog-definition.html",
    "type": "object",
    "properties": {
      "class": { 
        "description": "The default definition class for dialogs is ConfiguredFormDialogDefinition.",
        "type": "string",
        "default": "info.magnolia.ui.dialog.ConfiguredFormDialogDefinition"
      },
      "actions": { 
        "description": "Contains action definitions which define how the data is submitted. The actions are rendered to users as buttons. Typically you need at least commit and cancel actions.",
        "type": "object",
        "properties": {
          "commit": {
            "type": "object",
            "properties": {
              "class": { 
                "description": "Defines an action for saving a dialog. This handler can be overriden by extending from the default class.",
                "type": "string",
                "examples": [  "info.magnolia.ui.contentapp.action.CommitActionDefinition" ]
              },
              "$type": {
                "description": "Action with an @ActionType annotation which can be used for saving a dialog.",
                "type": "string",
                "examples": [  "commitAction" ]
              },
              "label": {
                "description": "Button label.",
                "type": "string",
                "examples": [  "Save changes" ]
              }
            },
            "additionalProperties": true
          },
          "cancel": {
            "type": "object",
            "properties": {
              "class": {
                "description": "Defines an action for canceling a dialog.",
                "type": "string",
                "examples": [  "info.magnolia.ui.contentapp.action.CloseActionDefinition" ]
              },
              "$type": {
                "description": "Action with an @ActionType annotation which can be used for canceling a dialog.",
                "type": "string",
                "examples": [  "closeAction" ]
              },
              "label": {
                "description": "Button label.",
                "type": "string",
                "examples": [  "Cancel" ]
              }
            },
            "additionalProperties": true
          }
        },
        "additionalProperties": true
      },
      "footerLayout": {
        "description": "Contains the action area configuration. Specifies the action component layout via LayoutDefinition.",
        "type": "object",
        "properties": {
          "$type": {
            "description": "Layout with an @LayoutType annotation. Set the layout of the form.",
            "type": "string",
            "examples": [ "declarativeLayout", "defaultEditorActionLayout", "form", "horizontal", "stacked", "tabbedLayout" ]
          }
        },
        "additionalProperties": true
      },
      "label": { 
        "description": "Dialog label displayed to editors. The value can be literal or a key of a message bundle. If you do not provide the property, Magnolia will fall back to a generated i18n key. If you do not want to have any label, set the property to an empty string such as label: '' in YAML.",
        "type": "string"
      },
      "light": { 
        "description": "When false, the dialog blacks out the background—you can only interact with the dialog until it is closed.",
        "type": "boolean"
      },
      "form": {
        "description": "Contains a form definition which defines the fields where content is entered.",
        "type": "object",
        "properties": {
          "properties": {
            "type": "object",
            "properties": {
              "text": {
                "$ref": "https://raw.githubusercontent.com/magnolia-community/definition-schemas/UI6/field.properties.text.json",
                "additionalProperties": true
              },
              "link": {
                "$ref": "https://raw.githubusercontent.com/magnolia-community/definition-schemas/UI6/field.properties.link.json",
                "additionalProperties": true
              }
            }
          }
        },
        "additionalProperties": true
      },
      "wide": { 
        "description": "When true, the dialog appears in wide mode using the maximum width of the viewport with a 20-pixel    padding on the left and right. See also the width property.",
        "type": "boolean" 
      },
      "width": { 
        "description": "Defines the dialog width. Possible values are small (700 pixels), medium (1000 pixels) and wide. For small and medium to work, the wide property has to be false.",
        "type": "string",
        "examples": [ "small", "medium" ]
      }
    },
    "additionalProperties": true
}